What is it about:
This repository focuses on building CNN models, both with and without transfer learning.

Contents of the Repository:

Team models_final.ipynb

Contains two CNN models:
	Aurele's model
	Paul's model (the chosen model)
Includes code and corresponding plots for both models.


transfer-learning-CNN-G3.ipynb
Contains two transfer learning models:
	EfficientNetB1
	ResNet101V2